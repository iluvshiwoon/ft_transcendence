{
  "name": "ft_transcendance",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel dev",
    "build": "pnpm --parallel build",
    "lint": "eslint ."
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "esbuild",
      "fastify-socket.io",
      "sharp"
    ]
  }
}
